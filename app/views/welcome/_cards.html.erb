<h3 class="title"><%= t("welcome.cards.title") %></h3>
<p><%= t("welcome.cards.subtitle") %>

<div class="row" data-equalizer data-equalizer-on="medium">
  <% @cards.each do |card| %>
    <%= render "card", card: card %>
  <% end %>
</div>
